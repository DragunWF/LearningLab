import { StyleSheet, View, Text } from "react-native";

function AddPlace() {
  return (
    <View>
      <Text>AddPlace</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default AddPlace;
